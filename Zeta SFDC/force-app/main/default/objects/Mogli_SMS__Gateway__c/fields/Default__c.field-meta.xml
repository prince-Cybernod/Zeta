<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Default__c</fullName>
    <defaultValue>false</defaultValue>
    <description>When checked, this is the Gateway that will populate Leads in the Default Gateway field. Only a single Gateway record can have this checked at a time.</description>
    <inlineHelpText>When checked, this is the Gateway that will populate Leads in the Default Gateway field. Only a single Gateway record can have this checked at a time.</inlineHelpText>
    <label>Default</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
