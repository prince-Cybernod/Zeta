<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>StatusProgressionValidation_SeatOffered</fullName>
    <active>false</active>
    <description>Enforces valid transitions from Seat Offered on School Application. Blocks all pre-lottery transitions and restricts post-lottery transitions to Accepted or Withdrawn/Declined.</description>
    <errorConditionFormula>AND(
    /* Bypass validation for authorized rollback users */
    NOT($Permission.Zeta_Rollback_Lottery),

    TEXT(PRIORVALUE(Status)) = &quot;Seat Offered&quot;,

    /* Only validate when the value is actually changing */
    TEXT(Status) &lt;&gt; TEXT(PRIORVALUE(Status)),

    NOT(
        OR(
            /* Always allowed */
            ISPICKVAL(Status, &quot;Withdrawn/Declined&quot;),

            /* Post-Lottery allowed transition */
            AND(Has_Lottery_Run__c, ISPICKVAL(Status, &quot;Seat Accepted&quot;))
        )
    )
)</errorConditionFormula>
    <errorDisplayField>Status</errorDisplayField>
    <errorMessage>When a School Application is in Seat Offered status, it may only progress to Seat Accepted after the lottery has run. At any time, it may be updated to Withdrawn/Declined.</errorMessage>
</ValidationRule>
