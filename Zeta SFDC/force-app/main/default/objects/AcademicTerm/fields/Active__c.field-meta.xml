<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>Evaluates to true if the current date falls between Start Date and End Date.</description>
    <formula>AND(
  TODAY() &gt;=  DATEVALUE(StartDate),
  TODAY() &lt;=  DATEVALUE(EndDate)
)</formula>
    <inlineHelpText>Evaluates to true if the current date falls between Start Date and End Date.</inlineHelpText>
    <label>Active</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
