<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>Returns true if the associated Application Timeline is active.</description>
    <formula>AND(
	NOT(ISNULL(ApplicationTimeline.Id)),
	ApplicationTimeline.Active__c = true
)</formula>
    <inlineHelpText>Returns true if the associated Application Timeline is active.</inlineHelpText>
    <label>Active</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
